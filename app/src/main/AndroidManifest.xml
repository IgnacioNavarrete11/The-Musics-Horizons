<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheMusicsHorizons">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TheMusicsHorizons.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons.NoActionBar" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons.NoActionBar" />
        <activity android:name=".activities.HomeActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons" />
        <activity android:name=".activities.ScanAudioActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons.NoActionBar" />
        <activity android:name=".activities.ScanVisualActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons.NoActionBar" />
        <activity android:name=".activities.ExploreEventsActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons" />
        <activity android:name=".activities.AudioResultsActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons" />
        <activity android:name=".activities.SongDetailActivity" android:exported="false" android:theme="@style/Theme.TheMusicsHorizons" />

    </application>

</manifest>
